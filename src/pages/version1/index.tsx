import { ReviewWorkspacePage } from "@/pages/home"

/** Version 1 — tags as a secondary sidebar, occurrence-level review. */
export function Version1Page() {
  return <ReviewWorkspacePage variant="sidebar" />
}
