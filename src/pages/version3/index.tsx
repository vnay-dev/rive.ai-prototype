import { ReviewWorkspacePage } from "@/pages/home"

/** Version 3 — same as v1 sidebar layout, with status shown on the action buttons. */
export function Version3Page() {
  return <ReviewWorkspacePage decisionStyle="button" variant="sidebar" />
}
